[pytest]
addopts = -p no:capture
# Default clean suite for backend + real-device runtime work.
# Simulator tests are temporarily excluded until their pending migration
# to the simplified canonical runtime state model is completed.
testpaths = backend/tests device_runtime/tests
asyncio_mode = auto
